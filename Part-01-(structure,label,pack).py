from tkinter import *

root = Tk() # starting

# Make label
label1 = Label(root, text='Hellow World!')

# everything have 2 parts : making & displaying 

# only space wanted, center at every size
label1.pack()

root.mainloop() # ending
